/*
 *   sldemo_sfun_ndarray_add.c Simple C-MEX S-function for function call.
 *
 *   ABSTRACT:
 *     The purpose of this sfunction is to call a simple legacy
 *     function during simulation:
 *
 *        void array3d_add(double y1[size(u1,1)][size(u1,2)][size(u1,3)], double u1[][][], double u2[][][], int32 size(u1,1), int32 size(u1,2), int32 size(u1,3))
 *
 *   Simulink version           : 7.6 (R2010b) 28-Jun-2010
 *   C source code generated on : 30-Jun-2010 04:12:01

 * THIS S-FUNCTION IS GENERATED BY THE LEGACY CODE TOOL AND MAY NOT WORK IF MODIFIED

 */

/*
   %%%-MATLAB_Construction_Commands_Start
   def = legacy_code('initialize');
   def.SFunctionName = 'sldemo_sfun_ndarray_add';
   def.OutputFcnSpec = 'void array3d_add(double y1[size(u1,1)][size(u1,2)][size(u1,3)], double u1[][][], double u2[][][], int32 size(u1,1), int32 size(u1,2), int32 size(u1,3))';
   def.HeaderFiles = {'ndarray_ops.h'};
   def.SourceFiles = {'ndarray_ops.c'};
   def.IncPaths = {'sldemo_lct_src'};
   def.SrcPaths = {'sldemo_lct_src'};
   legacy_code('sfcn_cmex_generate', def);
   legacy_code('compile', def);
   %%%-MATLAB_Construction_Commands_End
 */

/*
 * Must specify the S_FUNCTION_NAME as the name of the S-function.
 */
#define S_FUNCTION_NAME                sldemo_sfun_ndarray_add
#define S_FUNCTION_LEVEL               2

/*
 * Need to include simstruc.h for the definition of the SimStruct and
 * its associated macro definitions.
 */
#include "simstruc.h"

/*
 * Specific header file(s) required by the legacy code function.
 */
#include "ndarray_ops.h"

/* Function: mdlInitializeSizes ===========================================
 * Abstract:
 *    The sizes information is used by Simulink to determine the S-function
 *    block's characteristics (number of inputs, outputs, states, etc.).
 */
static void mdlInitializeSizes(SimStruct *S)
{
  /* Number of expected parameters */
  ssSetNumSFcnParams(S, 0);

  /*
   * Set the number of pworks.
   */
  ssSetNumPWork(S, 0);

  /*
   * Set the number of dworks.
   */
  if (!ssSetNumDWork(S, 0))
    return;

  /*
   * Set the number of input ports.
   */
  if (!ssSetNumInputPorts(S, 2))
    return;

  /*
   * Configure the input port 1
   */
  ssSetInputPortDataType(S, 0, SS_DOUBLE);
  ssSetInputPortDimensionInfo(S, 0, DYNAMIC_DIMENSION);
  ssSetInputPortComplexSignal(S, 0, COMPLEX_NO);
  ssSetInputPortDirectFeedThrough(S, 0, 1);
  ssSetInputPortAcceptExprInRTW(S, 0, 0);
  ssSetInputPortOverWritable(S, 0, 0);
  ssSetInputPortOptimOpts(S, 0, SS_REUSABLE_AND_LOCAL);
  ssSetInputPortRequiredContiguous(S, 0, 1);

  /*
   * Configure the input port 2
   */
  ssSetInputPortDataType(S, 1, SS_DOUBLE);
  ssSetInputPortDimensionInfo(S, 1, DYNAMIC_DIMENSION);
  ssSetInputPortComplexSignal(S, 1, COMPLEX_NO);
  ssSetInputPortDirectFeedThrough(S, 1, 1);
  ssSetInputPortAcceptExprInRTW(S, 1, 0);
  ssSetInputPortOverWritable(S, 1, 0);
  ssSetInputPortOptimOpts(S, 1, SS_REUSABLE_AND_LOCAL);
  ssSetInputPortRequiredContiguous(S, 1, 1);

  /*
   * Set the number of output ports.
   */
  if (!ssSetNumOutputPorts(S, 1))
    return;

  /*
   * Configure the output port 1
   */
  ssSetOutputPortDataType(S, 0, SS_DOUBLE);
  ssSetOutputPortDimensionInfo(S, 0, DYNAMIC_DIMENSION);
  ssSetOutputPortComplexSignal(S, 0, COMPLEX_NO);
  ssSetOutputPortOptimOpts(S, 0, SS_REUSABLE_AND_LOCAL);
  ssSetOutputPortOutputExprInRTW(S, 0, 0);

  /*
   * Register reserved identifiers to avoid name conflict
   */
  if (ssRTWGenIsCodeGen(S)) {
    /*
     * Register reserved identifier for OutputFcnSpec
     */
    ssRegMdlInfo(S, (char*)"array3d_add", MDL_INFO_ID_RESERVED, 0, 0, (void*)
                 ssGetPath(S));
  }

  /*
   * Set the option for ND signals support.
   */
  ssAllowSignalsWithMoreThan2D(S);

  /*
   * This S-function can be used in referenced model simulating in normal mode.
   */
  ssSetModelReferenceNormalModeSupport(S, MDL_START_AND_MDL_PROCESS_PARAMS_OK);

  /*
   * Set the number of sample time.
   */
  ssSetNumSampleTimes(S, 1);

  /*
   * All options have the form SS_OPTION_<name> and are documented in
   * matlabroot/simulink/include/simstruc.h. The options should be
   * bitwise or'd together as in
   *   ssSetOptions(S, (SS_OPTION_name1 | SS_OPTION_name2))
   */
  ssSetOptions(S,
               SS_OPTION_USE_TLC_WITH_ACCELERATOR |
               SS_OPTION_CAN_BE_CALLED_CONDITIONALLY |
               SS_OPTION_EXCEPTION_FREE_CODE |
               SS_OPTION_WORKS_WITH_CODE_REUSE |
               SS_OPTION_SFUNCTION_INLINED_FOR_RTW |
               SS_OPTION_DISALLOW_CONSTANT_SAMPLE_TIME);
}

/* Function: mdlInitializeSampleTimes =====================================
 * Abstract:
 *    This function is used to specify the sample time(s) for your
 *    S-function. You must register the same number of sample times as
 *    specified in ssSetNumSampleTimes.
 */
static void mdlInitializeSampleTimes(SimStruct *S)
{
  ssSetSampleTime(S, 0, INHERITED_SAMPLE_TIME);
  ssSetOffsetTime(S, 0, FIXED_IN_MINOR_STEP_OFFSET);

#if defined(ssSetModelReferenceSampleTimeDefaultInheritance)

  ssSetModelReferenceSampleTimeDefaultInheritance(S);

#endif

}

#define MDL_SET_INPUT_PORT_DIMENSION_INFO
#if defined(MDL_SET_INPUT_PORT_DIMENSION_INFO) && defined(MATLAB_MEX_FILE)

/* Function: mdlSetInputPortDimensionInfo =================================
 * Abstract:
 *    This method is called with the candidate dimensions for an input port
 *    with unknown dimensions. If the proposed dimensions are acceptable, the
 *    method should go ahead and set the actual port dimensions.
 *    If they are unacceptable an error should be generated via
 *    ssSetErrorStatus.
 *    Note that any other input or output ports whose dimensions are
 *    implicitly defined by virtue of knowing the dimensions of the given
 *    port can also have their dimensions set.
 *
 */
static void mdlSetInputPortDimensionInfo(SimStruct *S, int_T portIndex, const
  DimsInfo_T *dimsInfo)
{
/* /* Set input port dimension */
if(!ssSetInputPortDimensionInfo(S, portIndex, dimsInfo)) return;

/* Verify input port 1 dimension */
if ((portIndex == 0) && (ssGetInputPortNumDimensions(S, 0) != 3)) {
  ssSetErrorStatus(S, "Input 1: number of dimensions must be 3");
}

/* Verify input port 2 dimension */
if ((portIndex == 1) && (ssGetInputPortNumDimensions(S, 1) != 3)) {
  ssSetErrorStatus(S, "Input 2: number of dimensions must be 3");
}

/* Set output port 1 dimension */
{
DECL_AND_INIT_DIMSINFO(dimsInfo);
boolean_T hasDynSize = 0;
int_T i;
int_T iDims[3];

/* Get the specified dimensions */
iDims[0] =  ((ssGetInputPortNumDimensions(S, 0) >= 1) ? ssGetInputPortDimensions(S, 0)[0] : DYNAMICALLY_SIZED);
iDims[1] =  ((ssGetInputPortNumDimensions(S, 0) >= 2) ? ssGetInputPortDimensions(S, 0)[1] : DYNAMICALLY_SIZED);
iDims[2] =  ((ssGetInputPortNumDimensions(S, 0) >= 3) ? ssGetInputPortDimensions(S, 0)[2] : DYNAMICALLY_SIZED);

/* Look for unknown dimensions */
for (i = 0; i < 3; i++) {
  if (iDims[i]==DYNAMICALLY_SIZED) {
    hasDynSize |= 1;
  }
}

/* Set dimensions if all aren't dynamically sized */
if (!hasDynSize) {
  iDims[2] = (iDims[2]==1 ? 2 : iDims[2]);
  dimsInfo.numDims = 3;
  dimsInfo.dims = &iDims[0];
  dimsInfo.width = 1;
  for (i = 0; i < dimsInfo.numDims; i++) {
     dimsInfo.width *= iDims[i];
  }

  ssSetOutputPortDimensionInfo(S, 0, &dimsInfo);
}

}

}
#endif

#define MDL_SET_OUTPUT_PORT_DIMENSION_INFO
#if defined(MDL_SET_OUTPUT_PORT_DIMENSION_INFO) && defined(MATLAB_MEX_FILE)
/* Function: mdlSetOutputPortDimensionInfo ================================
 * Abstract:
 *    This method is called with the candidate dimensions for an output port
 *    with unknown dimensions. If the proposed dimensions are acceptable, the
 *    method should go ahead and set the actual port dimensions.
 *    If they are unacceptable an error should be generated via
 *    ssSetErrorStatus.
 *    Note that any other input or output ports whose dimensions are
 *    implicitly defined by virtue of knowing the dimensions of the given
 *    port can also have their dimensions set.
 *
 */
static void mdlSetOutputPortDimensionInfo(SimStruct *S, int_T portIndex, const DimsInfo_T *dimsInfo)
{
/* Set output port dimension */
if(!ssSetOutputPortDimensionInfo(S, portIndex, dimsInfo)) return;

/* Verify output port 1 dimension */
{
boolean_T hasDynSize = 0;
boolean_T hasBadDim = 0;
int_T i;
int_T iDims[3];

/* Get the specified dimensions */
iDims[0] =  ((ssGetInputPortNumDimensions(S, 0) >= 1) ? ssGetInputPortDimensions(S, 0)[0] : DYNAMICALLY_SIZED);
iDims[1] =  ((ssGetInputPortNumDimensions(S, 0) >= 2) ? ssGetInputPortDimensions(S, 0)[1] : DYNAMICALLY_SIZED);
iDims[2] =  ((ssGetInputPortNumDimensions(S, 0) >= 3) ? ssGetInputPortDimensions(S, 0)[2] : DYNAMICALLY_SIZED);

/* Compare the candidate dimensions against specification */
if (dimsInfo->numDims != 3) {
  hasBadDim = 1;
} else {
  for (i = 0; i < 3; i++) {
    if (iDims[i]==DYNAMICALLY_SIZED) {
      hasDynSize |= 1;
    } else {
      if (dimsInfo->dims[i] != iDims[i]) {
        hasBadDim |= 1;
      }
    }
  }
}

if (!hasDynSize && hasBadDim) {
   ssSetErrorStatus(S, "Invalid output port dimensions. The output signal 1 must be a 3D signal.");
}

}

}
#endif

#define MDL_SET_DEFAULT_PORT_DIMENSION_INFO
#if defined(MDL_SET_DEFAULT_PORT_DIMENSION_INFO) && defined(MATLAB_MEX_FILE)
/* Function: mdlSetDefaultPortDimensionInfo ===============================
 * Abstract:
 *    This method is called when there is not enough information in your
 *    model to uniquely determine the port dimensionality of signals
 *    entering or leaving your block. When this occurs, Simulink's
 *    dimension propagation engine calls this method to ask you to set
 *    your S-functions default dimensions for any input and output ports
 *    that are dynamically sized.
 *
 *    If you do not provide this method and you have dynamically sized ports
 *    where Simulink does not have enough information to propagate the
 *    dimensionality to your S-function, then Simulink will set these unknown
 *    ports to the 'block width' which is determined by examining any known
 *    ports. If there are no known ports, the width will be set to 1.
 *
 */
static void mdlSetDefaultPortDimensionInfo(SimStruct *S)
{
/* Set input port 1 default dimension */
  if (ssGetInputPortWidth(S, 0) == DYNAMICALLY_SIZED) {
  DECL_AND_INIT_DIMSINFO(dimsInfo);
  int_T dims[3];

dims[0] = 1;
dims[1] = 1;
   dims[2] = 2;
  dimsInfo.numDims = 3;
  dimsInfo.width = 2;
  dimsInfo.dims = &dims[0];
  ssSetInputPortDimensionInfo(S, 0, &dimsInfo);
  }

/* Set input port 2 default dimension */
  if (ssGetInputPortWidth(S, 1) == DYNAMICALLY_SIZED) {
  DECL_AND_INIT_DIMSINFO(dimsInfo);
  int_T dims[3];

dims[0] = 1;
dims[1] = 1;
   dims[2] = 2;
  dimsInfo.numDims = 3;
  dimsInfo.width = 2;
  dimsInfo.dims = &dims[0];
  ssSetInputPortDimensionInfo(S, 1, &dimsInfo);
  }

/* Set output port 1 default dimension */
{
DECL_AND_INIT_DIMSINFO(dimsInfo);
boolean_T hasDynSize = 0;
int_T i;
int_T iDims[3];
int_T oDims[3];

/* Get the specified dimensions */
iDims[0] =  ((ssGetInputPortNumDimensions(S, 0) >= 1) ? ssGetInputPortDimensions(S, 0)[0] : DYNAMICALLY_SIZED);
iDims[1] =  ((ssGetInputPortNumDimensions(S, 0) >= 2) ? ssGetInputPortDimensions(S, 0)[1] : DYNAMICALLY_SIZED);
iDims[2] =  ((ssGetInputPortNumDimensions(S, 0) >= 3) ? ssGetInputPortDimensions(S, 0)[2] : DYNAMICALLY_SIZED);

/* Get the actual dimensions and compare against specification */
for (i = 0; i < 3; i++) {
  if (i < ssGetOutputPortNumDimensions(S, 0)) {
    oDims[i] = ssGetOutputPortDimensions(S, 0)[i];
  } else {
    oDims[i] = DYNAMICALLY_SIZED;
  }

  if (oDims[i]==DYNAMICALLY_SIZED) {
    hasDynSize |= 1;
    oDims[i] = (iDims[i]==DYNAMICALLY_SIZED ? 1 : iDims[i]);
  } else {
    if (oDims[i]!=iDims[i]) {
      ssSetErrorStatus(S, "Output 1: incompatible dimension during forward propagation versus default dimension");
    }
  }
}

/* Set default dimensions if some are dynamically sized */
if (hasDynSize) {
  oDims[2] = (oDims[2]==1 ? 2 : oDims[2]);
  dimsInfo.numDims = 3;
  dimsInfo.dims = &oDims[0];
  dimsInfo.width = 1;
  for (i = 0; i < dimsInfo.numDims; i++) {
    dimsInfo.width *= oDims[i];
  }

  ssSetOutputPortDimensionInfo(S, 0, &dimsInfo);
}

}

}
#endif

/* Function: mdlOutputs ===================================================
 * Abstract:
 *    In this function, you compute the outputs of your S-function
 *    block. Generally outputs are placed in the output vector(s),
 *    ssGetOutputPortSignal.
 */
static void mdlOutputs(SimStruct *S, int_T tid)
{
/*
 * Get access to Parameter/Input/Output/DWork/size information
 */
  real_T *u1 = (real_T *) ssGetInputPortSignal(S, 0);
  real_T *u2 = (real_T *) ssGetInputPortSignal(S, 1);
  real_T *y1 = (real_T *) ssGetOutputPortSignal(S, 0);
  int32_T u1_dim1 = (int32_T)  ((ssGetInputPortNumDimensions(S, 0) >= 1) ? ssGetInputPortDimensions(S, 0)[0] : 0);
  int32_T u1_dim2 = (int32_T)  ((ssGetInputPortNumDimensions(S, 0) >= 2) ? ssGetInputPortDimensions(S, 0)[1] : 0);
  int32_T u1_dim3 = (int32_T)  ((ssGetInputPortNumDimensions(S, 0) >= 3) ? ssGetInputPortDimensions(S, 0)[2] : 0);

/*
 * Call the legacy code function
 */
array3d_add( y1, u1, u2, u1_dim1, u1_dim2, u1_dim3);
}

/* Function: mdlTerminate =================================================
 * Abstract:
 *    In this function, you should perform any actions that are necessary
 *    at the termination of a simulation.
 */
static void mdlTerminate(SimStruct *S)
{
}


/*
 * Required S-function trailer
 */
#ifdef    MATLAB_MEX_FILE
# include "simulink.c"
#else
# include "cg_sfun.h"
#endif
