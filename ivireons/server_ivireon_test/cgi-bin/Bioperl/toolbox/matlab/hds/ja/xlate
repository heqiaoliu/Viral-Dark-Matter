% toolbox/matlab/hds/ja/xlate.

%% matlab\hds/@hds/@AbstractDataSet/addGridPoints.m
<(addGridPoints)Invalid syntax or unknown variable.
>(addGridPoints)無効な構文または不明な変数です。
<(addGridPoints)No grid variable detected
>(addGridPoints)検出されたグリッド変数はありません
<(addGridPoints)Number of arguments does not match number of grid dimensions.
>(addGridPoints)引数の数は、グリッドの次元の数と一致しません。
<(addGridPoints)Number of grid points must be the same for all variables along a grid dimension.
>(addGridPoints)グリッドの点数は、グリッドの次元に沿ったすべての変数に対して同じでなければなりません。
<(addGridPoints)Variable %s does not belong to the grid.
>(addGridPoints)変数 %s は、グリッドに属していません。
<(addGridPoints)Xj must be a cell array with as many entries as variables along grid dimension.
>(addGridPoints)Xj は、グリッドの次元に沿った変数と同じ入力を持つセル配列でなければなりません。

%% matlab\hds/@hds/@AbstractDataSet/addsample.m
<(addsample)Invalid list of variable name/value pairs.
>(addsample)変数の名前と値のペアのリストが無効です。
<(addsample)New sample must be specified as a scalar structure.
>(addsample)新規サンプルは、スカラーの構造体として指定しなければなりません。
<(addsample)Undefined value for grid variable %s
>(addsample)グリッドの変数 %s に対する未定義の値
<(addsample)Use ADDGRIDPOINTS for multi-dimensional grids.
>(addsample)多次元グリッドについては、ADDGRIDPOINTS を使用してください。

%% matlab\hds/@hds/@AbstractDataSet/delsample.m
<(delsample)DELSAMPLE currently does not supported multi-dimensional grids.
>(delsample)DELSAMPLE は、現在、多次元グリッドをサポートしていません。
<(delsample)Indices should be a vector of integers.
>(delsample)インデックスは、整数のベクトルでなければなりません。
<(delsample)Indices should be within the range of 1 and %s.\n
>(delsample)インデックスは、1 と %s の範囲内にある必要があります。\n
<(delsample)MIN and MAX values should be numerical scalar values.
>(delsample)MIN と MAX 値は、数値のスカラー値でなければなりません。
<(delsample)More input arguments are required.
>(delsample)より多くの入力引数が必要です。
<(delsample)no data points have been removed.
>(delsample)削除されたデータ点がありません。

%% matlab\hds/@hds/@AbstractDataSet/feval.m
<(feval)Variable %s cannot be found or is not accessible to FEVAL.
>(feval)変数 %s が見つからないか、FEVAL にアクセスできません。

%% matlab\hds/@hds/@AbstractDataSet/fevalh.m
<(fevalh)Function values must have uniform size over the entire grid.
>(fevalh)関数値は、グリッド全体で一定のサイズでなければなりません。
<(fevalh)Variable %s cannot be found or is not accessible to FEVALH.
>(fevalh)変数 %s が見つからないか、FEVALH にアクセスできません。

%% matlab\hds/@hds/@AbstractDataSet/getContainer.m
<(getContainer)Unknown variable %s
>(getContainer)不明な変数 %s

%% matlab\hds/@hds/@AbstractDataSet/getGridPoints.m
<(getGridPoints)Only applicable to root-level grid variables.
>(getGridPoints)ルートレベルのグリッド変数にのみ適用可能です。

%% matlab\hds/@hds/@AbstractDataSet/getMetaData.m
<(getMetaData)Can only retrieve metadata for root-level variables.
>(getMetaData)ルートレベルの変数のメタデータのみ取り出すことができます。

%% matlab\hds/@hds/@AbstractDataSet/getsample.m
<(getsample)Invalid vector of indices.
>(getsample)インデックスのベクトルが無効です。
<(getsample)Not available until grid is fully specified.
>(getsample)グリッドが完全に指定されるまで利用できません。
<(getsample)Number of index vectors does not match number of grid dimensions.
>(getsample)インデックス ベクトルの数が、グリッドの次元の数と一致しません。

%% matlab\hds/@hds/@AbstractDataSet/setContainer.m
<(setContainer)Specified container must be a subclass of hds.ValueArray.
>(setContainer)指定したコンテナは、hds.ValueArray のサブクラスでなければなりません。

%% matlab\hds/@hds/@AbstractDataSet/setMetaData.m
<(setMetaData)Third argument must be a @metadata object.
>(setMetaData)3 番目の引数は、@metadata オブジェクトでなければなりません。

%% matlab\hds/@hds/@AbstractDataSet/setgrid.m
<(setgrid)Number of grid dimensions cannot be redefined.
>(setgrid)グリッドの次元数を再定義できません。

%% matlab\hds/@hds/@AbstractDataSet/setsample.m
<(setsample)Data for variable %s is of the wrong type or size.
>(setsample)変数 %s に対するデータは、正しくないタイプかサイズです。
<(setsample)Invalid data for dependent data sets with alias %s
>(setsample)エイリアス %s を持つ依存データ セットに対する無効なデータ
<(setsample)Size mismatch between sample array and specified portion of the grid.
>(setsample)サンプルの配列と指定したグリッド位置の間でサイズが一致しません。

%% matlab\hds/@hds/@AbstractDataSet/slice.m
<(slice)Number of subscripts exceeds number of grid dimensions.
>(slice)添字の数がグリッドの次元数を超えました。

%% matlab\hds/@hds/@AbstractDataSet/sliceVar.m
<(sliceVar)Only available for top-level variables and links.
>(sliceVar)最上位の変数とリンクに対してのみ利用可能です。

%% matlab\hds/@hds/@AbstractDataSet/sortgrid.m
<(sortgrid)No grid variable is available. Sort aborted.
>(sortgrid)利用可能なグリッド変数はありません。並べ替えを中止しました。
<(sortgrid)Unknown mode.
>(sortgrid)不明なモードです。

%% matlab\hds/@hds/@AbstractDataSet/subsref_TMP.m
<(subsref_TMP)Invalid indexing syntax into data set object.
>(subsref_TMP)データ セット オブジェクトへの構文のインデックス付けが無効です。

%% matlab\hds/@hds/@AbstractDataSet/utCheckArraySize.m
<(utCheckArraySize)Bad or ambiguous data sizing for variable %s
>(utCheckArraySize)変数 %s に対して正しくないか、あいまいなデータのサイズ処理
<(utCheckArraySize)Duplicate grid points are not allowed.
>(utCheckArraySize)重複するグリッド点は、許可されません。
<(utCheckArraySize)Multi-dimensional data: first specify the grid dimensionality using SETGRID.
>(utCheckArraySize)多次元データ: 最初に SETGRID を使ってグリッドの次元を指定します。
<(utCheckArraySize)Scalar assignment not allowed until grid dimensions are fully specified.
>(utCheckArraySize)スカラーの代入は、グリッドの次元が完全に指定されるまで許可されません。

%% matlab\hds/@hds/@ArrayAdaptor/hdsGetSize.m
<(hdsGetSize)ArrayAdaptor is an abstract class: hdsGetSize must be overloaded
>(hdsGetSize)ArrayAdaptor は抽象クラスです: hdsGetSize はオーバーロードされなければなりません

%% matlab\hds/@hds/@ArrayAdaptor/hdsGetSlice.m
<(hdsGetSlice)ArrayAdaptor is an abstract class: hdsGetSlice must be overloaded
>(hdsGetSlice)ArrayAdaptor は抽象クラスです: hdsGetSlice はオーバーロードされなければなりません

%% matlab\hds/@hds/@ArrayAdaptor/hdsNewArray.m
<(hdsNewArray)ArrayAdaptor is an abstract class: hdsNewArray must be overloaded
>(hdsNewArray)ArrayAdaptor は抽象クラスです: hdsNewArray はオーバーロードされなければなりません

%% matlab\hds/@hds/@ArrayAdaptor/hdsSetSlice.m
<(hdsSetSlice)ArrayAdaptor is an abstract class: hdsSetSlice must be overloaded
>(hdsSetSlice)ArrayAdaptor は抽象クラスです: hdsSetSlice はオーバーロードされなければなりません

%% matlab\hds/@hds/@LinkArray/checkLinks.m
<(checkLinks)All data sets in a data link must contain the same variables.
>(checkLinks)データのリンク内のすべてのデータ セットは、同じ変数を含まなければなりません。
<(checkLinks)Invalid value for data link %s. Expects cell array of data set handles.
>(checkLinks)データのリンク %s に対する値が無効です。データ セットのハンドルのセル配列が必要です。

%% matlab\hds/@hds/@LinkArray/schema.m
<(schema)Variable "%s" appears multiple times when "%s" is made transparent.\nResetting transparency to 'off'.
>(schema)変数 "%s" は、"%s" を透過にした場合に複数回現れます。\n透明性を 'off' に再設定してください。

%% matlab\hds/@hds/@TestLog/addGridPoints.m
<(addGridPoints)Grid cannot be modified for test points.
>(addGridPoints)グリッドは、テスト ポイントに対して修正できません。

%% matlab\hds/@hds/@TestLog/addlink.m
<(addlink)Data links not available for test points.
>(addlink)データのリンクは、テスト ポイントに対して利用できません。

%% matlab\hds/@hds/@TestLog/addvar.m
<(addvar)Value of variable %s could not be written.
>(addvar)変数 %s の値を書き込めません。

%% matlab\hds/@hds/@TestLog/display.m
<(display)Test point.
>(display)テスト ポイント。

%% matlab\hds/@hds/@TestLog/schema.m
<(schema)Invalid value for Storage property.
>(schema)Storage プロパティに対する値が無効です。

%% matlab\hds/@hds/@TestLog/setContents.m
<(setContents)Cannot write to file %s
>(setContents)ファイル %s に書き込めません

%% matlab\hds/@hds/@TestLog/setgrid.m
<(setgrid)Grid is not defined for test points.
>(setgrid)グリッドはテスト ポイントに対して定義されません。

%% matlab\hds/@hds/@ValueArray/utArray2Cell.m
<(utArray2Cell)Array size not compatible with sample size
>(utArray2Cell)配列サイズがサンプル サイズと一致しません。

%% matlab\hds/@hds/@ValueArray/utCell2Array.m
<(utCell2Array)Sample size is not uniform.
>(utCell2Array)サンプル サイズが一定でありません。

%% matlab\hds/@hds/@dataset/dataset.m
<(dataset)DATASET constructor takes between zero and two arguments.
>(dataset)DATASET のコンストラクターは、0 と 2 の間の引数を与えます。

%% matlab\hds/@hds/@dataset/delvar.m
<(delvar)Cannot remove a grid variable
>(delvar)グリッド変数を削除できません

%% matlab\hds/@hds/@dataset/display.m
<(display)Hierarchical data set with %d data points
>(display)%d のデータ点を持つ階層データ セット
<(display)Hierarchical data set with %s grid of data points
>(display)データ点の %s のグリッドを持つ階層データ セット

%% matlab\hds/@hds/@dataset/subsasgn.m
<(subsasgn)Cannot modify multiple data sets at once.
>(subsasgn)複数のデータ セットを同時に変更することはできません。
<(subsasgn)Invalid assignment into uninitialized variable %s
>(subsasgn)初期化されていない変数 %s への無効な代入
<(subsasgn)Invalid assignment syntax into data set object.
>(subsasgn)データ セットのオブジェクトへの構文の代入が無効です。

%% matlab\hds/@hds/@spreadsheet/display.m
<(display)Spreadsheet with %d data points and %d variables.
>(display)%d 個のデータ点と %d 個の変数を持つスプレッドシート。

%% matlab\hds/@hds/@spreadsheet/setgrid.m
<(setgrid)Spreadsheets have one-dimensional grids.
>(setgrid)スプレッドシートは、1 次元のグリッドを持っています。

%% matlab\hds/@hds/@spreadsheet/slice.m
<(slice)Variables cannot be repeated.
>(slice)変数は繰り返せません。

%% matlab\hds/@hds/@spreadsheet/spreadsheet.m
<(spreadsheet)SPREADSHEET constructor takes between zero and one argument.
>(spreadsheet)SPREADSHEET のコンストラクターは、0 と 1 の間の引数を与えます。

%% matlab\hds/@hds/@variable/display.m
<(display)Data set variable %s\n
>(display)データ セット変数 %s\n

%% matlab\hds/@hds/@variable/locate.m
<(locate)Unknown variable %s.
>(locate)不明な変数 %s です。

%% matlab\hds/@hds/@variable/variable.m
<(variable)First argument must be a string or a hds.variable object.
>(variable)1 番目の引数は、文字列か hds.variable オブジェクトでなければなりません。
<(variable)Last character of variable name cannot be an underscore.
>(variable)変数名の最後の文字を下線にはできません。
<(variable)Variable name must be specified.
>(variable)変数名が指定されなければなりません。

%% matlab/hds/@hds/@AbstractDataSet/utCheckArraySize.m
<(utCheckArraySize)Use cell arrays for grid variables with non-scalar values.
>(utCheckArraySize)非スカラー値を使ったグリッド変数の場合はセル配列を使用してください。

%% matlab/hds/@hds/@AbstractDataSet/utCheckArraySize.m
<(utCheckArraySize)First dimension(s) of data array for variable %s must match grid size or\ntotal number of samples. %s
>(utCheckArraySize)変数 %s のデータ配列の最初の次元は、グリッド サイズ\n、またはサンプルの総数に一致しなければなりません。%s
<(utCheckArraySize)Last dimension(s) of data array for variable %s must match grid size or\ntotal number of samples. %s
>(utCheckArraySize)変数 %s のデータ配列の最後の次元は、グリッド サイズ、\nまたはサンプルの総数に一致しなければなりません。%s
<(utCheckArraySize)Size of data array for grid variable %s does not match grid size.
>(utCheckArraySize)グリッド変数 %s のデータ配列のサイズが、グリッド サイズに一致しません。
<(utCheckArraySize)Size of data array for variable %s does not match number of samples in data set.\n%s
>(utCheckArraySize)変数 %s のデータ配列のサイズが、データ セット内のサンプル数と一致しません。\n%s

%% matlab/hds/@hds/@MATFileContainer/getArray.m
<(getArray)Data file %s cannot be found or does not contain variable %s.
>(getArray)データ ファイル %s が見つからないか、変数 %s を含んでいません。

%% matlab/toolbox/matlab/hds/@hds/@AbstractDataSet/delsample.m
<(delsample)value format does not match the format used by the variable.
>(delsample)値の形式が変数で使用されている形式と一致しません。
<(delsample)variable has to be a vector.
>(delsample)変数はベクトルでなければなりません。
<(delsample)variable name must be a string of characters.
>(delsample)変数名は文字列でなければなりません。

% Copyright 2007-2010 The MathWorks, Inc.
