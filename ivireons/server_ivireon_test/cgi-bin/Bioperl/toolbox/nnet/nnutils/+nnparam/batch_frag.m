%Batch Frag (batch_frag) function parameter
%
%  <a href="matlab:doc nnparam.batch_frag">batch_frag</a> is a <a href="matlab:doc nntrain">training function</a> parameter.
%  It must be a positive scalar.
%
%  <a href="matlab:doc nnparam.batch_frag">batch_frag</a> is used to indicate no fragmentation of the
%  batch of training time-series (with a value of 0) or to indicate
%  that a time-series is to be broken up for purposes of training
%  (values greater than zero).
%
%  This parameter is used by <a href="matlab:doc trainbfg">trainbfg</a>.
 
